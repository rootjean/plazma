# ─────────────────────────────────────────────────────────────
# Variables de entorno de Plazma Ideas  (PLANTILLA — sin secretos)
#
# EN PRODUCCIÓN (VPS):
#   1. Copia esta plantilla:   cp .env.example .env
#   2. Pega tu clave real en GROQ_API_KEY dentro de .env
#   El archivo .env NO se sube al repositorio (está en .gitignore).
# ─────────────────────────────────────────────────────────────

# Clave de API de Groq para el asistente de IA del sitio.
# Consíguela en https://console.groq.com/keys
GROQ_API_KEY=
